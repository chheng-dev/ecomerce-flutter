import 'package:flutter/material.dart';

class KidTabWidget extends StatelessWidget {
  const KidTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}