import 'package:flutter/material.dart';

class MenTabWidget extends StatelessWidget {
  const MenTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}